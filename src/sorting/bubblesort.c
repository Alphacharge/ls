#include "ft_ls.h"

// static void	swap_chars(char **a, char **b) {
// 	char	*tmp = *a;

// 	*a = *b;
// 	*b = tmp;
// }

// void	bubblesort(char **input, int n, bool direction)
// {
// 	for (int i = 0; i < n - 1; i++) {
// 		for (int j = 0; j < n - i - 1; j++) {
// 			if (!direction && ft_strcmp(input[j], input[j + 1]) > 0)
// 				swap_chars(&input[j], &input[j + 1]);
// 			if (direction && ft_strcmp(input[j], input[j + 1]) < 0)
// 				swap_chars(&input[j], &input[j + 1]);
// 		}
// 	}
// }
